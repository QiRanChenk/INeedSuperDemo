# OpenAI-compatible endpoint. Can also be set in the shell UI (settings), which takes precedence.
LLM_BASE_URL=https://api.deepseek.com
LLM_API_KEY=sk-xxx
LLM_MODEL=deepseek-chat

# Shell port
PORT=3000
